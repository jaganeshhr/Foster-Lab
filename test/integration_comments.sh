#!/usr/bin/env bash
# Comments (giscus/disqus) are demonstrated on this template's example blog
# posts only. This site has intentionally removed the blog (nav item, posts,
# and blog-only config) per a lab-site content decision, so there is no
# comments-bearing page left to build and assert against. Skipping rather
# than deleting the script keeps it available as a reference/reintroducible
# check if the blog is ever brought back.
echo "comments integration test skipped: blog (and its demo posts) removed from this site"
exit 0
