#!/usr/bin/env bash
# The distill demo relies on this template's example distill blog post. This
# site has intentionally removed the blog (nav item, posts, and blog-only
# config), so there is no distill demo page left to build and assert against.
# Skipping rather than deleting the script keeps it available as a
# reference/reintroducible check if the blog is ever brought back.
echo "distill integration test skipped: blog (and its demo posts) removed from this site"
exit 0
